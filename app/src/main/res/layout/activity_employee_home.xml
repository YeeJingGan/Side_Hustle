<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_grey"
    tools:context=".activites.employee.EmployeeHomeActivity">

    <include layout="@layout/layout_common_top_search_filter" />

    <include layout="@layout/layout_common_bottom" />

    <View
        android:id="@+id/toolbar_bottom_divider"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="@dimen/toolbar_height"
        android:background="?android:attr/listDivider"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ListView
        android:id="@+id/employee_home_job_list_view"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="@dimen/employee_home_list_view_margin"
        android:layout_marginTop="@dimen/employee_home_list_view_margin"
        android:layout_marginEnd="@dimen/employee_home_list_view_margin"
        android:layout_marginBottom="@dimen/employee_home_list_view_margin"
        android:divider="@android:color/transparent"
        android:dividerHeight="15dp"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navbar_top_divider"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/toolbar_bottom_divider"
        tools:listitem="@layout/layout_employee_home_job_item">

    </ListView>

    <View
        android:id="@+id/bottom_navbar_top_divider"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:layout_marginBottom="@dimen/footerDividerHeight"
        android:background="?android:attr/listDivider"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <ImageButton
        android:id="@+id/favorite_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/favorite_button_margin"
        android:layout_marginBottom="@dimen/favorite_button_margin"
        android:background="@drawable/background_circle_theme_color"
        android:contentDescription="@string/favorite_button_description"
        android:padding="@dimen/favorite_button_padding"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navbar_top_divider"
        app:layout_constraintEnd_toEndOf="parent"
        app:srcCompat="@drawable/ic_favorite_24px"
        app:tint="@color/white" />

</androidx.constraintlayout.widget.ConstraintLayout>